
<% const _view = typeof view === 'string' ? view : '404'

%>
<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">Home</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="<%= view === 'fundamentalModes' ? 'active' : '' %>"><a href="/fundamentalModes">Fundamental Modes</a></li>
        <li class="<%= view === 'composingModes' ? 'active' : '' %>"><a href="/composingModes">Composing Fundamental Modes</a></li>
        <li class="<%= view === 'fourTuples' ? 'active' : '' %>"><a href="/fourTuples">All 4-Tuples and Their Compositions</a></li>
        <!-- <li class="<%= view === 'morphs' ? 'active' : '' %>"><a href="/morphs">Morphs</a></li> -->
        
        
        
        <!-- <li><a href="/plank-graph ">Plank Graph</a></li> -->
        <% for (let rank = 0; rank <= 3; rank++) { %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Rank <%= rank %><span class="caret"></span></a>
            <ul class="dropdown-menu">
              <% for (let size = rank; size <= 5; size++ ) { %>
                <li><a href="/morph/size/<%= size %>/rank/<%= rank %>">n = <%= size %></a></li>
              <% } %>
            </ul>
          </li>
        <% } %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
